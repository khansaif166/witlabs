---
const testimonials = [
  {
    initials: 'AG',
    name: 'Founder',
    role: 'ASWAD GLOBAL',
    company: 'Business Consulting',
    quote: 'Clean, professional, and easier to present to prospects.',
  },
  {
    initials: 'IP',
    name: 'Director',
    role: 'IMPACT PACKAGING',
    company: 'Manufacturing',
    quote: 'Clear process. Strong B2B understanding.',
  },
  {
    initials: 'KE',
    name: 'Founder',
    role: 'KOTA EXPERTS',
    company: 'Education',
    quote: 'Better website. Better student enquiries.',
  },
];
---

<section class="section-shell bg-cream-dark text-ink">
  <div class="mx-auto max-w-[1440px]">
    <div class="grid gap-8 lg:grid-cols-[0.8fr_1.2fr] lg:items-end">
      <div>
        <p class="section-kicker reveal text-green">Trusted By Founders</p>
        <h2 class="reveal reveal-delay-1 mt-4 display-tight text-[clamp(2.15rem,4.4vw,4.2rem)] font-semibold">
          Proof from real work.
        </h2>
      </div>
      <p class="reveal reveal-delay-2 max-w-md text-base leading-7 text-ink-light lg:justify-self-end">
        Short notes from clients who trusted the work.
      </p>
    </div>

    <div class="testimonial-grid mt-12">
      {testimonials.map((item, index) => (
        <figure class:list={['testimonial-card reveal', index > 0 && `delay-${index}`]}>
          <blockquote>
            <span aria-hidden="true">“</span>
            <p>{item.quote}</p>
          </blockquote>
          <figcaption>
            <div class="testimonial-avatar">{item.initials}</div>
            <div>
              <p>{item.name}</p>
              <small>{item.role} / {item.company}</small>
            </div>
          </figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>

<style>
  .testimonial-grid {
    display: grid;
    gap: 16px;
  }

  .testimonial-card {
    display: flex;
    min-height: 280px;
    flex-direction: column;
    justify-content: space-between;
    border: 1px solid rgba(28, 25, 23, 0.08);
    border-radius: 8px;
    background: #FFFFFF;
    padding: clamp(24px, 4vw, 40px);
    transition: transform 0.35s var(--ease-premium), box-shadow 0.35s var(--ease-premium);
  }

  .testimonial-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-soft);
  }

  .testimonial-card blockquote > span {
    display: block;
    color: rgba(45, 106, 79, 0.2);
    font-size: 64px;
    line-height: 0.7;
  }

  .testimonial-card blockquote p {
    margin-top: 22px;
    color: #1C1917;
    font-size: clamp(1.05rem, 1.65vw, 1.45rem);
    font-weight: 600;
    letter-spacing: -0.045em;
    line-height: 1.18;
  }

  .testimonial-card figcaption {
    margin-top: 48px;
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .testimonial-avatar {
    display: grid;
    height: 46px;
    width: 46px;
    place-items: center;
    border-radius: 999px;
    background: #1C1917;
    color: #FFFFFF;
    font-weight: 700;
  }

  .testimonial-card figcaption p {
    font-weight: 700;
  }

  .testimonial-card figcaption small {
    margin-top: 3px;
    display: block;
    color: #78716C;
    font-family: "DM Mono", monospace;
    font-size: 10px;
    letter-spacing: 0.08em;
  }

  @media (min-width: 900px) {
    .testimonial-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }
</style>
