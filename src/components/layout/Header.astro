---
interface Props {
  transparent?: boolean;
}

const { transparent = false } = Astro.props;
const pathname = Astro.url.pathname;

const navLinks = [
  { label: 'Home', href: '/' },
  { label: 'Services', href: '/services/', hasDropdown: true },
  { label: 'Case Studies', href: '/case-studies/' },
  { label: 'About', href: '/about/' },
  { label: 'Insights', href: '/insights/' },
  { label: 'Contact', href: '/contact/' },
];

const serviceLinks = [
  { label: 'Website Design & Development', href: '/services/#website-design', icon: 'monitor' },
  { label: 'SEO & Organic Growth', href: '/services/#seo', icon: 'trending-up' },
  { label: 'Digital Marketing', href: '/services/#digital-marketing', icon: 'megaphone' },
  { label: 'Performance Marketing', href: '/services/#performance', icon: 'target' },
  { label: 'App Development', href: '/services/#app-development', icon: 'smartphone' },
  { label: 'Branding & Identity', href: '/services/#branding', icon: 'layers' },
  { label: 'Automation', href: '/services/#automation', icon: 'zap' },
  { label: 'AI Chatbots', href: '/services/#chatbots', icon: 'bot' },
];

function isActive(href: string): boolean {
  if (href === '/') return pathname === '/';
  const normalizedHref = href.endsWith('/') ? href : `${href}/`;
  const normalizedPath = pathname.endsWith('/') ? pathname : `${pathname}/`;
  return normalizedPath.startsWith(normalizedHref);
}
---

<header
  id="site-header"
  class:list={[
    'fixed top-0 left-0 right-0 z-50 transition-all duration-300 text-cream',
    transparent
      ? 'bg-transparent border-b border-transparent [&.scrolled]:bg-cream/95 [&.scrolled]:text-ink [&.scrolled]:border-border [&.scrolled]:backdrop-blur'
      : 'bg-cream/95 text-ink border-b border-border backdrop-blur',
  ]}
  aria-label="Site header"
>
  <nav class="container h-16 md:h-[72px] flex items-center justify-between" aria-label="Main navigation">
    <a href="/" aria-label="WitLabs - Home" class="flex items-center gap-2.5 group shrink-0">
      <span class="flex h-7 w-7 items-center justify-center border border-current/25 font-mono text-[11px] font-bold tracking-[-0.08em] text-current">
        WL
      </span>
      <span class="font-sans font-semibold text-xl text-current tracking-tight transition-colors group-hover:text-green">
        WitLabs
      </span>
    </a>

    <ul class="hidden lg:flex items-center gap-1" role="list">
      {navLinks.map((link) => (
        <li class={link.hasDropdown ? 'relative group' : ''}>
          <a
            href={link.href}
            class:list={[
              'relative flex items-center gap-1 px-3 py-2 text-body-sm font-medium rounded-lg transition-colors after:absolute after:left-3 after:right-3 after:-bottom-0.5 after:h-[1.5px] after:rounded-full after:bg-green-bright after:transition-transform after:duration-200',
              isActive(link.href)
                ? 'text-green-bright [&_span]:text-green after:scale-x-100'
                : 'text-current/60 hover:text-current hover:bg-current/5 after:scale-x-0',
            ]}
            aria-current={isActive(link.href) ? 'page' : undefined}
          >
            {link.label}
            {link.hasDropdown && (
              <svg width="12" height="12" viewBox="0 0 12 12" fill="currentColor" class="opacity-50 transition-transform group-hover:rotate-180" aria-hidden="true">
                <path d="M2 4l4 4 4-4" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round" />
              </svg>
            )}
          </a>

          {link.hasDropdown && (
            <div
              class="absolute top-full left-1/2 -translate-x-1/2 pt-2 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 translate-y-1 group-hover:translate-y-0"
              role="menu"
              aria-label="Services submenu"
            >
              <div class="w-64 rounded-2xl border border-border bg-cream p-3 text-ink shadow-xl">
                <p class="text-label uppercase text-muted px-3 py-2 mb-1">Our Services</p>
                {serviceLinks.map((service) => (
                  <a
                    href={service.href}
                    class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-body-sm text-ink-light hover:bg-green-light hover:text-green transition-colors"
                    role="menuitem"
                  >
                    <span class="w-5 h-5 flex items-center justify-center text-green opacity-70" aria-hidden="true">→</span>
                    {service.label}
                  </a>
                ))}
              </div>
            </div>
          )}
        </li>
      ))}
    </ul>

    <div class="hidden lg:flex items-center gap-3">
      <a href="tel:+917984301015" class="text-body-sm text-current/50 hover:text-current transition-colors font-mono">
        +91 79843 01015
      </a>
      <a
        href="/contact/"
        class="inline-flex items-center gap-1.5 border border-current/30 text-current text-sm font-medium px-5 py-2 rounded-full hover:bg-green hover:border-green hover:text-white transition-colors"
      >
        Book a Strategy Call
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
          <path d="M2 7h10M8 3l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
        </svg>
      </a>
    </div>

    <button
      id="menu-toggle"
      class="lg:hidden flex flex-col gap-1.5 p-2 rounded-lg hover:bg-current/10 transition-colors"
      type="button"
      aria-label="Open navigation menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span class="w-5 h-px bg-current block transition-all" id="bar-1"></span>
      <span class="w-5 h-px bg-current block transition-all" id="bar-2"></span>
      <span class="w-3 h-px bg-current block transition-all" id="bar-3"></span>
    </button>
  </nav>
</header>

<div
  id="mobile-menu"
  class="fixed inset-0 z-40 bg-cream flex flex-col pt-20 px-6 pb-8 translate-x-full transition-transform duration-300 lg:hidden"
  aria-hidden="true"
>
  <nav class="flex-1" aria-label="Mobile navigation">
    <ul class="space-y-1" role="list">
      {navLinks.map((link) => (
        <li>
          <a
            href={link.href}
            class="flex items-center justify-between py-4 text-3xl font-semibold text-ink hover:text-green border-b border-border transition-colors"
          >
            {link.label}
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
              <path d="M4 10h12M12 5l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
            </svg>
          </a>
        </li>
      ))}
    </ul>
  </nav>

  <div class="mt-8 space-y-3">
    <p class="text-muted text-caption">Get in touch</p>
    <a href="tel:+917984301015" class="block text-ink text-body-lg font-medium">+91 79843 01015</a>
    <a
      href="/contact/"
      class="block w-full text-center bg-green text-white font-medium py-4 rounded-2xl text-body-md"
    >
      Book a Free Strategy Call
    </a>
  </div>
</div>

<script is:inline>
  const toggle = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');
  const bar1 = document.getElementById('bar-1');
  const bar2 = document.getElementById('bar-2');
  const bar3 = document.getElementById('bar-3');
  let isOpen = false;

  function setMenuState(open) {
    isOpen = open;
    menu?.classList.toggle('translate-x-full', !isOpen);
    menu?.setAttribute('aria-hidden', String(!isOpen));
    toggle?.setAttribute('aria-expanded', String(isOpen));
    document.body.style.overflow = isOpen ? 'hidden' : '';

    bar1?.classList.toggle('rotate-45', isOpen);
    bar1?.classList.toggle('translate-y-2', isOpen);
    bar2?.classList.toggle('opacity-0', isOpen);
    bar3?.classList.toggle('-rotate-45', isOpen);
    bar3?.classList.toggle('-translate-y-2', isOpen);
    bar3?.classList.toggle('w-5', isOpen);
  }

  toggle?.addEventListener('click', () => {
    setMenuState(!isOpen);
  });

  menu?.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => setMenuState(false));
  });

  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape' && isOpen) {
      setMenuState(false);
    }
  });
</script>
